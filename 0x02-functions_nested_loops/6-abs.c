#include "main.h"

/**
 * _abs-Function that computes the absolute value of an integer 
 * x: integral input 
 * Return:Absolute value of x
 */
int _abs(int x)
{	
if (x < 0)
	{
		x *= -1;
	}
	return (x);
}

