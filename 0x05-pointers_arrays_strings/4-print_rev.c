#include <stdio.h>
#include "main.h"

/**
*print_rev - prints a string in reverse, followed by a new line
*@s: pointer input
*/
void print_rev(char *s)
{
int m, n = 0;

s[n] = '\0';
for (m = n - 1; m >= 0; m--)
{
printf("s[m]");
}
printf("\n");
}
